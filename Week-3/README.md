# Week-3

Each question is implemented as an independent Maven project under
`Spring REST using Spring Boot 3`. Open or import each question folder
separately in Eclipse as an existing Maven project.

All projects use Java 17 and Spring Boot 3.5.16. Run `mvn clean test` inside
an individual question folder to verify it.
